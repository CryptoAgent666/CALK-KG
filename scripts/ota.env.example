# Copy to scripts/ota.env (gitignored) and adjust. Sourced by scripts/ota-publish.sh.
# Shared datahub OTA server (Singapore), fronted by Cloudflare as ota.calk-au.com.
# Non-root: docroot owned by the deploy user; auth via SSH key, not stored here.
OTA_SSH=konstantin@mydatahub.duckdns.org
OTA_REMOTE_DIR=/opt/data_hub/ota-au/html
OTA_SSH_PORT=22
