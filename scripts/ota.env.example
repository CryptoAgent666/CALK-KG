# Copy to scripts/ota.env (gitignored) and adjust. Sourced by scripts/ota-publish.sh.
# Shared network OTA server (AU VPS). Auth is via SSH key, not stored here.
OTA_SSH=root@176.97.68.234
OTA_REMOTE_DIR=/var/www/www-root/data/www/ota.calk-au.com
OTA_SSH_PORT=22
