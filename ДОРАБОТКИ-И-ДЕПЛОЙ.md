# calk.kg — инструкция по доработкам и деплою

Памятка после пилота констант (2026-06-17). Читать вместе с `CONSTANTS-PILOT-2026-06.md` (что и
почему менялось) и `CLAUDE.md` (контекст проекта). Все суммы — сом (KGS).

**Текущее состояние:** 25 правок применены в **рабочее дерево** (НЕ задеплоено). `npm run build`
проверен — проходит (exit 0, оба i18n-чанка собираются). Леджер: 428 констант, 35/35 калькуляторов.

---

## ⚠️ Перед любым коммитом: в репо два слоя изменений

Рабочее дерево содержит **~150 уже изменённых файлов, которые были ДО пилота** (незакоммиченное
обновление, поверх коммита `98f60a0 "…data updates for April 2026"`). **Это не мои правки.** Сначала
реши, что с ними делать (закоммитить/откатить/разобрать) — отдельно от пилота.

**Файлы, которые тронул ТОЛЬКО пилот констант (можно закоммитить отдельным коммитом):**
```
src/data/sickLeaveData.ts          src/pages/ElectricityCalculatorPage.tsx
src/data/trafficFines.ts           src/pages/FamilyBenefitCalculatorPage.tsx
src/i18n/translations-ru.ts        src/pages/GasCalculatorPage.tsx
src/i18n/translations-ky.ts        src/pages/HeatingCalculatorPage.tsx
                                   src/pages/PensionCalculatorPage.tsx
# новые файлы:
src/data/regulatory-constants.canonical.json
CONSTANTS-PILOT-2026-06.md  CLAUDE.md  ДОРАБОТКИ-И-ДЕПЛОЙ.md
```
Коммит только пилота:
```bash
git add src/data/sickLeaveData.ts src/data/trafficFines.ts \
        src/i18n/translations-ru.ts src/i18n/translations-ky.ts \
        src/pages/{Electricity,FamilyBenefit,Gas,Heating,Pension}CalculatorPage.tsx \
        src/data/regulatory-constants.canonical.json \
        CONSTANTS-PILOT-2026-06.md CLAUDE.md ДОРАБОТКИ-И-ДЕПЛОЙ.md
git commit -m "constants 2026: исправлены неверные gov-значения (МЗП, гарантия вкладов, пенсия,
пособие, тарифы ЭЭ/газ/тепло) + проза RU/KY; добавлен аудит-леджер"
```
**Не коммить:** файлы `*.bak-kgpilot` (мои бэкапы — удалить после ревью: `rm src/**/*.bak-kgpilot`) и
папку `dist/` (вывод сборки, должна быть в `.gitignore`).

---

## ЧАСТЬ A. ДЕПЛОЙ (то, что уже исправлено)

### A1. Сборка (проверено — проходит)
```bash
npm install          # если давно не ставил зависимости
npm run build        # vite build → generate-static-html.js → generate-sitemap.js → dist/
npm run preview      # локально посмотреть прод-сборку перед публикацией
```
Артефакт — папка **`dist/`** (статика + сгенерированный HTML на калькулятор + sitemap).

### A2. Публикация — определи свой хостинг
В репо есть и `public/_redirects` (конвенция **Cloudflare Pages / Netlify**), и `nginx.conf`
(самохостинг/VPS). CI-workflow нет. Деплой = один из двух путей:

- **Путь 1 — Git-connected статик-хостинг (наиболее вероятно для calk.kg).** Если сайт привязан к
  GitHub в Cloudflare Pages или Netlify, деплой = просто `git push origin main` → хостинг сам
  соберёт и опубликует. `_redirects` подхватится автоматически.
  Проверить: `dig +short NS calk.kg` (cloudflare.com → CF Pages) или панель Cloudflare/Netlify.
- **Путь 2 — ручная заливка / VPS.** `npm run build`, затем выложить содержимое `dist/` на хост
  (или в `root` из `nginx.conf`). Cloudflare Pages вручную: `npx wrangler pages deploy dist
  --project-name <project>`.

> ❓ **Подтверди, какой путь у calk.kg** (панель хостинга / DNS), прежде чем публиковать. Если push в
> `main` уже триггерит сборку — достаточно закоммитить + запушить (см. выше).

### A3. Smoke-test после деплоя (открыть на проде, RU и KY)
Проверь, что исправленные значения видны:
- **Зарплата** — МЗП **3 280** сом (не 2 500).
- **Вклады** — гарантия **1 000 000** сом (не 200 000).
- **Пенсия** — базовая часть **3 170**; **Пособие на ребёнка** — **1 500** (ГМД 1 500).
- **Электричество** — население без льготы **4.17**, коммерч. 4.33; **Газ** коммерч. **24.25**;
  **Отопление** Бишкек соц. **1 560** Гкал.
- **Патент** — лимит **30 млн**; **Единый налог** — лимит **8 млн**.
- Переключить язык RU↔KY — числа совпадают.

---

## ЧАСТЬ B. ДОРАБОТКИ (отложено в пилоте — структурное, нужна твоя проверка логики)

Порядок — по приоритету (B1–B2 меняют неверный расчёт, делать первыми). После любой правки:
`npx tsc --noEmit` → `npm run build` → smoke-test → деплой. **Чинить COMPUTE, затем зеркалить в
`translations-ru.ts` + `translations-ky.ts`.**

### B1. Больничный (`src/data/sickLeaveData.ts` + `SickLeaveCalculatorPage.tsx`) — расчёт неверный
По Пост. Кабмина **№434** (cbd.minjust.gov.kg/12601):
- **Брекеты стажа неверны.** Сейчас 4 диапазона дают 60/80/100/100. Должно быть **3**: стаж
  **до 5 лет = 60%**, **5–8 лет = 80%**, **свыше 8 лет = 100%**. Т.е. текущий «3–5 лет = 80%» →
  должно быть 60%; «5–8 лет = 100%» → должно быть 80%. Поправь массив `EXPERIENCE` (paymentPercent).
- **Первые дни.** `firstDays` (строка ~182) и расчёт говорят «первые **3** дня — работодатель».
  Должно быть **первые 10 рабочих дней — работодатель**, с 11-го — Соцфонд.
- **Потолок.** `MAX_MONTHLY_WAGE = 50000` (строка 37) → с 11-го дня выплата Соцфонда ограничена
  **10 000 сом/мес**. Завязать cap на «с 11-го дня», а не на всю базу.
- Обнови соответствующие строки в `translations-ru.ts`/`-ky.ts` (sick_*: проценты, «10 дней»).

### B2. Стипендии (`src/data/scholarshipData.ts`) — значения и семантика
- `academic.baseAmount = 3000` (строка 10) → **800** сом/мес (базовая академическая).
- `president.baseAmount = 8000` (строка 31) → **60 000**, но это **единоразовая** выплата, не
  ежемесячная. Нельзя просто заменить число: добавь флаг/семантику «one-time» (иначе калькулятор
  умножит на месяцы). Проверь, как Page трактует `baseAmount`, и обработай разовые отдельно.
- Прочие (named/government/rector/social) — суммы не подтверждены офиц. источником → оставить как
  есть и пометить, либо взять из приказа Кабмина о стипендиях 2025/26.

### B3. Статьи штрафов ПДД (`src/data/trafficFines.ts`) — ~19 неверных номеров статей
Суммы в основном верны, но поле `article` ссылается не на ту статью **Кодекса о правонарушениях**.
По актуальному Кодексу **№128 (ред. 23.04.2026)** ориентировочно: **ст.187** — превышение скорости;
**ст.188** — общие нарушения водителя (ремень, телефон, разметка, обочина, поворотник, свет, детское
кресло, проезд на красный); **ст.184** — управление без прав/документов; **ст.193** — опьянение.
Сейчас часть припаркована под ст.182/186/179/189/184 ошибочно (строки 54, 70, 114, 122, 130, 138,
156–172, 182–218, 292, 300, 320 — кандидаты).
> ⚠️ **Сверь номера со свежим Кодексом №128 на cbd.minjust.gov.kg перед правкой** — это юридические
> ссылки, не угадывать. Суммы трогать не нужно (кроме `phone_driving` 1 500→3 000, уже исправлено).

### B4. Нарративная проза газа/электричества (`translations-ru.ts` + `-ky.ts`) — связанная арифметика
Отложено, т.к. примеры считают итоги от тарифа (просто заменить число — сломать итог). Нужен
согласованный пересчёт в обоих языках:
- **Газ**: население **22.7041** сом/м³ (уже в compute). Ключи `gas_example1_*` (тариф 14.50→22.70,
  итог ~174→~272 при 12 м³), `gas_example2_*`/`gas_heating_*` (11.60→22.70, итог 5 800→~11 350 при
  500 м³, сезонные ~35–40 тыс → ~70–80 тыс), `gas_bishkek_rate`/`gas_osh_rate` (9.50/8.80→22.70),
  блок `gas_vs_electricity_*`.
- **Электричество**: `electricity_tariff_general_rate` / `_text` — сейчас «единый 0.77», должно быть
  тариф населения **1.64 сом/кВт·ч до 700 кВт·ч, 2.94 свыше** (compute уже такой).
- **Зарплата (пример при МЗП)**: `salary_article_mrot_*` (gross 2 500→3 280, соцфонд −250→−328,
  налог −225, «на руки» ~2 025) — но: МЗП 3 280 **ниже НМД 14 970**, значит подоходный налог при МЗП
  должен быть **0**. Сначала реши вопрос с НМД в расчёте зарплаты (см. B5-прим.), потом правь пример.

### B5. Прочее
- **Алименты — минимум не реализован.** Текст говорит «минимум 30% от МЗП = 984 сом на 1 ребёнка»
  (`alimony_minimum_note`, уже обновлён), но compute (`AlimonyCalculatorPage.tsx`) не применяет пол.
  Добавь floor: если `income×доля < 0.30×3280`, брать минимум.
- **НМД в расчёте зарплаты.** `SalaryCalculatorPage.tsx` считает налог как `(gross−соцфонд)×ставка`
  без вычета НМД (14 970). Проза описывает вычет НМД. Реши, как должно быть, и приведи к одному.
- **102 «uncertain» + 61 новый «unverified»** (патент по видам деят-сти уже полные; тарифы ЖКХ по
  городам, штрафы, госпошлины, турсбор) — сверка на **Tier-2** (квартальный прогон) и по календарю.
- **Мёртвый файл.** `src/i18n/translations.ts` (13 778 неиспользуемых ключей, никто не импортирует) —
  можно удалить, чтобы не путать. Перед удалением: `grep -rn "from.*i18n/translations'" src` (должно
  быть пусто), затем `git rm src/i18n/translations.ts` + `npm run build`.

---

## ЧАСТЬ C. Цикл мониторинга (сервер DATA_HUB)
- **Tier-1** еженедельно: `calk-kg-monitor-config.json` (281 gov / 56 источников, хеш только по
  `*.gov.kg`). Алерт о дрейфе → правишь **здесь** (COMPUTE → RU+KY) → `npm run build` → деплой.
- **Tier-2** ежеквартально (8 янв/апр/июл/окт): LLM-сверка, закрывает uncertain/unverified.
- Дашборд: mydatahub.duckdns.org/dashboard → 🧮 Калькуляторы → calk.kg = «Полный пилот».
- Источники KG: налоги sti.gov.kg/salyk.kg; Соцфонд sf.gov.kg; пособия mlsp.gov.kg; Кабмин
  cabinet.gov.kg; законы cbd.minjust.gov.kg; таможня customs.gov.kg; ГРС grs.gov.kg; НБКР nbkr.kg;
  вклады deposit.kg; ипотека gik.kg; тарифы ТЭК regultek.gov.kg.

## Чеклист
- [ ] Разобрать ~150 pre-existing незакоммиченных файлов (отдельно от пилота)
- [ ] Закоммитить правки пилота (список выше), удалить `*.bak-kgpilot`
- [ ] `npm run build` → `npm run preview` → деплой (определить хостинг A2)
- [ ] Smoke-test проде (A3), RU+KY
- [ ] Доработки B1→B2 (неверный расчёт) → B3/B4/B5 → редеплой
- [ ] (опц.) удалить мёртвый `translations.ts`
