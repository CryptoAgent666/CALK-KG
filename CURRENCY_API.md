# 💱 API обновления курсов валют

## 🎯 Проблема и решение

**Проблема**: API НБКР (`https://www.nbkr.kg/XML/daily.xml`) не поддерживает CORS, поэтому браузер блокирует прямые запросы.

**Решение**: Создана Netlify Function (`/netlify/functions/currency-rates.ts`), которая:
- ✅ Получает курсы от НБКР на сервере (CORS не проблема)
- ✅ Кэширует данные на 1 час
- ✅ Отдает JSON с правильными CORS заголовками
- ✅ Имеет fallback на актуальные курсы при ошибке

---

## 🚀 Как работает

### На продакшене (после деплоя):
```
Фронтенд → /.netlify/functions/currency-rates → НБКР API
            ↓
         JSON с курсами
```

### Локально (для разработки):
```
Фронтенд → http://localhost:8888/.netlify/functions/currency-rates → НБКР API
```

---

## 🛠 Локальная разработка

### 1. Установка Netlify CLI (если еще не установлено):
```bash
npm install -g netlify-cli
```

### 2. Запуск с Netlify Dev:
```bash
netlify dev
```

Это запустит:
- **Vite dev-сервер** на `http://localhost:5173`
- **Netlify Functions** на `http://localhost:8888/.netlify/functions/`

### 3. Тестирование API напрямую:
```bash
curl http://localhost:8888/.netlify/functions/currency-rates
```

Ответ:
```json
{
  "date": "2026-01-17",
  "rates": {
    "USD": {
      "code": "USD",
      "name": "Доллар США",
      "rate": 87.25,
      "nominal": 1
    },
    "EUR": { ... },
    "RUB": { ... },
    "KZT": { ... },
    "CNY": { ... }
  },
  "timestamp": "2026-01-17T12:00:00.000Z",
  "cached": false
}
```

---

## 📦 Деплой на Netlify

### Автоматический деплой:
1. Push в GitHub
2. Netlify автоматически соберет и задеплоит

### Ручной деплой:
```bash
npm run build
netlify deploy --prod
```

---

## 🔧 Конфигурация

### netlify.toml
```toml
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"  ← Важно!

[dev]
  functions = "netlify/functions"   ← Важно!
```

### Переменные окружения (не требуются)
API НБКР публичный, токены не нужны ✅

---

## 🎨 Как фронтенд использует API

### src/hooks/useCurrencyRates.ts
```typescript
const apiUrl = import.meta.env.PROD 
  ? '/.netlify/functions/currency-rates'           // Production
  : 'http://localhost:8888/.netlify/functions/currency-rates'; // Dev

const response = await fetch(apiUrl);
const data = await response.json();
```

---

## 📊 Кэширование

- **Серверный кэш**: 1 час (в памяти функции)
- **Browser cache**: Нет (всегда свежие данные)
- **Обновление**: Автоматическое при каждом запросе после истечения кэша

---

## ⚡ Производительность

- **Первый запрос**: ~500-1000ms (получение от НБКР)
- **Кэшированный**: ~50-100ms
- **Fallback**: Мгновенно (при ошибке НБКР)

---

## 🐛 Troubleshooting

### Проблема: "Failed to fetch"
**Решение**: Убедитесь, что запущен `netlify dev`, а не просто `npm run dev`

### Проблема: CORS ошибка в production
**Решение**: Проверьте, что в `netlify.toml` указано `functions = "netlify/functions"`

### Проблема: Функция не деплоится
**Решение**: Убедитесь, что установлен `@netlify/functions`:
```bash
npm install --save-dev @netlify/functions
```

---

## 🔮 Будущие улучшения

- [ ] Сохранение истории курсов в Supabase
- [ ] Scheduled function для обновления каждый день в 10:00
- [ ] Webhook уведомления о резких изменениях курса
- [ ] Прогноз курса на основе истории

---

## 📝 Лицензия

MIT
